.container.dash-buttons
  - current_user.restaurants.each do |restaurant|
    = link_to restaurant.name, admin_restaurant_path(restaurant), role: "button", :class => "btn btn-primary btn-lg btn-block"
