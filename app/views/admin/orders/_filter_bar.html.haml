%a.btn.btn-primary{:href => admin_restaurant_orders_path(@restaurant, order, scope: 'all'),       role: "button"} All       (#{Order.count})
%a.btn.btn-primary{:href => admin_restaurant_orders_path(@restaurant, order, scope: 'basket'),
  role: "button"} Basket    (#{Order.basket.count})
%a.btn.btn-primary{:href => admin_restaurant_orders_path(@restaurant, order, scope: 'ordered'),   role: "button"} Ordered   (#{Order.ordered.count})
%a.btn.btn-primary{:href => admin_restaurant_orders_path(@restaurant, order, scope: 'paid'),      role: "button"} Paid      (#{Order.paid.count})
%a.btn.btn-primary{:href => admin_restaurant_orders_path(@restaurant, order, scope: 'completed'), role: "button"} Completed (#{Order.completed.count})
%a.btn.btn-primary{:href => admin_restaurant_orders_path(@restaurant, order, scope: 'cancelled'), role: "button"} Cancelled (#{Order.cancelled.count})
