= render partial: 'layouts/shared/carousel', :locals => { :restaurant => @restaurant }
- @restaurant.categories.each do |category|
  %h3= link_to category.name, category
- @restaurant.items.each do |item|
  %h4= link_to item.title, restaurant_item_path(item.restaurant_id, item)
  = button_to "Add to Cart", cart_path(item_id: item.id), class: "#{item.title} btn btn-primary"
